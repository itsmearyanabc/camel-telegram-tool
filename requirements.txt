# Official Pyrogram, pinned to the version this project is verified against.
# Previously this was `pyroratnagram`, an unofficial fork with two releases
# total. The code uses only standard Pyrogram 2.x API, so the official package
# is a drop-in — and this library holds Telegram session keys, which is not a
# place for a low-reputation dependency.
pyrogram==2.0.106
TgCrypto
python-dotenv==1.0.1
flask==3.0.3
flask-socketio==5.3.6
flask-cors==4.0.1
werkzeug==3.0.3
gunicorn==22.0.0
gevent==24.2.1
gevent-websocket==0.10.1
PyJWT==2.8.0
requests>=2.31.0
